## Доступные скрипты

Для установки зависимостей выполните:

### `npm install`

Устанавливает все необходимые зависимости, указанные в package.json.

### `npm start`

Запускает приложение в режиме разработки.\
Откройте [http://localhost:3000](http://localhost:3000) для просмотра в браузере.

При внесении изменений в код страница будет автоматически перезагружаться.\

### `npm test`

Запускает тестовый раннер

## Настройка окружения

Перед запуском проекта скопируйте файл `.env.example` в `.env` и укажите необходимые значения переменных:
