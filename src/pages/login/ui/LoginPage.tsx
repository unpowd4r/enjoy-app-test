import { LoginForm } from 'features/auth';

import { LoginFormWrapper } from './LoginPage.styles';

export const LoginPage = () => {
  return (
    <LoginFormWrapper>
      <LoginForm />
    </LoginFormWrapper>
  );
};
